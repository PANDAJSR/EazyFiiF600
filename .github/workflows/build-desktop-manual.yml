name: Build Desktop Packages

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Linux packages
        if: runner.os == 'Linux'
        run: pnpm build && pnpm exec electron-builder --linux AppImage tar.gz --publish never

      - name: Build Windows portable package
        if: runner.os == 'Windows'
        run: pnpm build && pnpm exec electron-builder --win portable --x64 --publish never

      - name: Build macOS packages
        if: runner.os == 'macOS'
        run: pnpm build && pnpm exec electron-builder --mac dmg zip --publish never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-${{ runner.os }}
          path: release/**/*
          if-no-files-found: error
